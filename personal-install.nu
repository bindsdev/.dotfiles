# Install the additional tools that I use in my development and desktop environment, that others might not want.

def main [] {
  # install LSPs (YAML, BASH, marksman, shellcheck, clangd), debuggers (GDB, LLDB, BOCHS), GH CLI, Lazygit, tree, qemu, discord, brave browser, GHex, hexyl, RISCV and AARCH64 GCC toolchain/cross compiler, NASM, clone my opensource forks
}